import React from "react";
import Wrapper from "../shared/components/Wrapper";

const SectionCards = ({ tours }) => {
  console.log("tours");

  return <Wrapper>bsdjbjedh</Wrapper>;
};

export default SectionCards;
